10-25，写入IIC后要延时再读取，否则读不到。
10-26，IIC的第一页存系统配置信息，第二页和第三页存卡号，后面的存进门的时间记录。
    把数据读取到ram，再进行判断，初步完成。
11-3,修改客户端IP、服务器IP和端口后能立即生效，修改客户端的端口后无效。（或者重启w5500没有用），上电第一次就固定端口了。

对象：1.读头 2.继电器 3.门吸 4.网络 5.按键 6.IIC flash 7.SPI flash 8.实时时钟
待做：spi flash 的单字节写，页256字节写

头文件的接口尽量简单，相关的操作函数以函数指针形式放到结构体中。

door.c和param.c、bsp_led.c中的函数待完成。

准备将door和led合并，简化操作。完成各个模块的子函数。
TODO:
1-11,明天准备将参数的读取写了，然后把命令协议制定初版，供马做参考。
更新参数加type判断，只写入需要更新的值。修改本地ip后，要重新设置网络。
1-12如果时间等于0点，则多重卡等开门复原状态。
网络任务先检查状，然后接受数据，处理命令，直接发送回复消息。
1-13,500卡号的配置和发包方法待定，还有几个命令没写；设计操作系统中的任务
1-15，第一次烧完程序后，连上服务器，发送复位命令来复位;函数 os_itv_set 设置了周期性时间延迟的时间间隔,单位时钟节拍数，参数范围 1-0x7FFF；函数 os_dly_wait 用于任务的延迟。
TODO:等板子做好以后，和外设相关的程序都要一一测试。
状态灯的延时用50ms还行，10ms亮度不够。
不需要网络发送互斥信号量，因为在不同的任务中调用，不会冲突。
遇到问题：使用消息邮箱传递结构体。（已解决）
增加胁迫卡/码--报警消息指令，主动上传。

1-31：门吸反馈没插或者是没吸上的，都是高电平。只有在吸上时为低电平。